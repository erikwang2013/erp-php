# 版本对比

> Copyright (c) 2026 erik <erik@erik.xyz> — https://erik.xyz
>
> 统计数字由 `bash scripts/doc-stats.sh` 实时采集，文档中以 `<!-- stats:key=value -->` 标注，
> CI（`.github/workflows/ci.yml` 的 docs 作业）自动校验文档与代码事实一致，漂移即红。

开放ERP系统提供三个版本，适应不同规模企业的需求。

---

## 版本概览

| 维度 | 简化版 (Lite) | 标准版 (Standard) | 完整版 (Full) |
|------|:---:|:---:|:---:|
| 分支 | `lite` | `standard` | `full` |
| 数据表 | 62（规划值） | 72（规划值） | 227 <!-- stats:tables=227 --> |
| 控制器 | 48（规划值） | 42（规划值） | 159 <!-- stats:controllers=159 --> |
| 业务模块 | 6（规划值） | 6（规划值） | 23 <!-- stats:modules=23 --> |

> **统计口径**：仓库当前只实现完整版（Full）一套代码；Lite/Standard 列为产品规划值
> （无对应分支，见下文「分支策略」），不参与 doc-stats 校验。
> Full 列数字由 `scripts/doc-stats.sh` 实测（227 表 / 159 控制器 / 23 业务模块），
> 与 `docs/FUNCTIONS.md` 附录口径一致。
> **分支事实**（2026-09-22 实测 `git branch -a` + `git ls-remote --heads origin`）：
> 仓库本地与远程均只剩 `main` 一条分支；`lite` / `standard` / `full` 三分支**已删除**
> （2026-08-31 曾实测三分支并存、同停在 2026-08-17 提交 `eea90c0`、彼此无差异且落后 `main` 38 个提交）。
> 该归档提交仍在 `main` 历史中（`git merge-base --is-ancestor eea90c0 main` 成立），
> 即版本差异如今只能靠提交与 tag 追溯，仓库里已无版本分支可 checkout。

---

## v1.17.0 变更（2026-09-15）

> 版本定位不变：仓库仍只实现完整版（Full）一套代码；Lite/Standard 列为产品规划值，对应分支已归档冻结。

- **管理端从两套变三套**：Angular 22（`apps/angular/`）与 React 19 + Vite（`apps/react/`）加入，
  与既有 Flutter 3.x Web（`apps/flutter/`）并列，三端共用同一套 `/admin/v1`、`/api/v1`、`/open/v1` 接口。
- **全平台 13 语种**（zh/en/ja/ko/de/fr/es/pt/ru/ar/hi/bn/id）：
  - 后端响应消息 `resource/translations/<locale>/`——`zh_CN` 565 条、其余 11 个语种各 544 条、`en` 30 条
    （口径：三文件叶子条目，`validation.php` 的 `attributes` 字段标签计入、其组键不计——`zh_CN` 因译出 21 个字段标签而多 21 条。`en` 行「英文即 key」，词典近乎为空）
  - 管理端界面：Angular 源词典 1456 键、React 1451 键 × 11 个新语种；**按语种懒加载**、每语种各成一个 chunk
  - 生成器：`scripts/gen-be-locales.mjs`（后端）、`scripts/gen-fe-locales.mjs`（前端，`--app angular|react`）
  - 切换入口：顶栏**独立 globe 图标** + 个人中心下拉（两端一致）
- **Flutter 与 HarmonyOS 仍为中/英两语种**，未纳入本轮。
- **对下表的影响**：Full 列 163 表 / 122 控制器 / 19 业务模块 → **227 / 159 / 23**；
  完成度矩阵新增「多语言 (i18n)」行（模块行 44 → 45，后端 API 39 → 40，业务逻辑 33 → 34）；
  注：2026-09-15 合并矩阵中重复的「多租户」行后，模块行回落至 44（后端 API 39、业务逻辑 33），
  上句为 v1.17.0 当时的增量口径，保留不改。

## v1.4.0 变更（2026-09-05）

> 版本定位不变：仓库仍只实现完整版（Full）一套代码；Lite/Standard 列为产品规划值，对应分支已归档冻结。

- **全站路径版本化**：`/admin/*` → `/admin/v1/*`、`/api/*` → `/api/v1/*`、`/open/*` → `/open/v1/*`；
  例外仅 `GET /api/docs`（OpenAPI 文档）与 TMS webhook；RBAC 权限点按去掉版本段后的 `method.path` 鉴权，
  存量角色数据零迁移（commit `3ee1430`；`API-Version` 请求头控制已先期移除，commit `8276a1b`）。
- **P0 多组织与成本会计**：多组织独立核算（Company/LedgerPeriod）、合并报表引擎（期末汇率折算 + 子公司间抵销，
  快照优先落 FinanceConsolidationReport）、存货/生产成本核算（材料领用 + 成本归集）。
- **P1 制造执行与协同**：工序报工/计件工资/委外收发/产能负荷/批次序列追溯（M1/M2/M6/M3）、信用控制（F7）、
  审批流程画布（B3）、打印模板（B1）、HR 薪资（H1/H2）、设备点检扫码（E1）、项目成本（P1）。
- **P2 差异化与生态**：会员体系（C1）、票据台账与银企对账（F6）、进项池与数电票（F5，真实税局为适配点）、
  渠道多驱动与失败重试（B4）、自定义字段（B7）、多租户到期计费（B5——租户隔离中间件仍未注册，部分启用）、
  培训与社保（H3/H4）。
- **功能矩阵**：44 模块行中 33 行双 ✅；21 行标注 v1.4.0（含 1 行部分启用），详见 `docs/FUNCTIONS.md` §19。

> 变更明细见仓库根 `CHANGELOG.md`。

---

## 功能对比

### 系统管理

| 功能 | Lite | Standard | Full |
|------|:---:|:---:|:---:|
| 用户管理（CRUD + 批量 + 导入） | ✔ | ✔ | ✔ |
| 角色权限（RBAC 三级权限树） | ✔ | ✔ | ✔ |
| 系统配置（键值对） | ✔ | ✔ | ✔ |
| 操作审计（8 平台来源端检测） | ✔ | ✔ | ✔ |
| 文件上传 / Excel 导出 / PDF 导出 | ✔ | ✔ | ✔ |
| 健康检查 / Prometheus 指标 | ✔ | ✔ | ✔ |
| JWT 认证 + 点击验证码 | ✔ | ✔ | ✔ |
| 7 层纵深防御安全防护 | ✔ | ✔ | ✔ |
| 国际化 (i18n) 13 语种（Angular/React；Flutter/HarmonyOS 仍中/英） | — | — | ✔ |

### 商品与基础数据

| 功能 | Lite | Standard | Full |
|------|:---:|:---:|:---:|
| 商品档案 + 多规格 SKU | ✔ | ✔ | ✔ |
| 多单位换算 + 价格策略 | ✔ | ✔ | ✔ |
| 商品分类（树形） + 品牌 | ✔ | ✔ | ✔ |
| 多仓库 + 多库位 | ✔ | ✔ | ✔ |
| 供应商 / 客户档案 | ✔ | ✔ | ✔ |

### 采购管理

| 功能 | Lite | Standard | Full |
|------|:---:|:---:|:---:|
| 采购申请 + 审批 | ✔ | ✔ | ✔ |
| 采购订单 | ✔ | ✔ | ✔ |
| 采购收货（自动入库 + 生成应付） | ✔ | ✔ | ✔ |
| 采购退货 | ✔ | ✔ | ✔ |
| 供应商结算 | ✔ | ✔ | ✔ |

### 销售管理

| 功能 | Lite | Standard | Full |
|------|:---:|:---:|:---:|
| 报价单（支持转订单） | ✔ | ✔ | ✔ |
| 销售订单 | ✔ | ✔ | ✔ |
| 销售发货（自动出库 + 生成应收） | ✔ | ✔ | ✔ |
| 销售退货 | ✔ | ✔ | ✔ |
| 客户结算 + 毛利分析 | ✔ | ✔ | ✔ |

### 库存管理

| 功能 | Lite | Standard | Full |
|------|:---:|:---:|:---:|
| 实时库存（四维精度） | ✔ | ✔ | ✔ |
| 出入库流水 | ✔ | ✔ | ✔ |
| 批次追踪 + 序列号追踪 | ✔ | ✔ | ✔ |
| 库存调拨 | ✔ | ✔ | ✔ |
| 盘点管理（计划+动态） | ✔ | ✔ | ✔ |
| 库存预警（上下限告警） | ✔ | ✔ | ✔ |
| 移动加权平均成本核算 | ✔ | ✔ | ✔ |

### 财务管理

| 功能 | Lite | Standard | Full |
|------|:---:|:---:|:---:|
| 应收应付（自动生成 + 核销） | ✔ | ✔ | ✔ |
| 收款单 / 付款单 | ✔ | ✔ | ✔ |
| 现金银行日记账 | ✔ | ✔ | ✔ |
| 费用报销（提交 → 审批 → 打款） | ✔ | ✔ | ✔ |
| 利润表 | ✔ | ✔ | ✔ |
| 固定资产折旧 | — | — | ✔ |
| 税务管理（多税种配置） | — | — | ✔ |
| 多币种 + 汇率管理 | — | — | ✔ |
| 预算管理（预算 vs 实际对比） | — | — | ✔ |
| 成本中心 / 利润中心（树形核算） | — | — | ✔ |

### CRM

| 功能 | Lite | Standard | Full |
|------|:---:|:---:|:---:|
| 客户联系人管理 | ✔ | ✔ | ✔ |
| 跟进记录 | ✔ | ✔ | ✔ |
| 营销活动管理 | — | — | ✔ |
| 服务工单（优先级+分配+解决流程） | — | — | ✔ |
| 客户分析报表 | — | — | ✔ |

### 平台能力

| 功能 | Lite | Standard | Full |
|------|:---:|:---:|:---:|
| 审批工作流引擎 | — | — | ✔ |
| 消息通知系统 | — | — | ✔ |
| API 文档 (erikwang2013/apidoc-php) | ✔ | ✔ | ✔ |

### 扩展模块

| 功能 | Lite | Standard | Full |
|------|:---:|:---:|:---:|
| 项目管理（WBS/甘特图/工时） | — | — | ✔ |
| 人力资源（组织/考勤/薪资） | — | — | ✔ |
| 生产制造（BOM/MRP/工单/工艺） | — | — | ✔ |
| 自定义报表构建器 | — | — | ✔ |

---

## 适用场景

| 版本 | 推荐场景 |
|------|---------|
| **Lite** | 中小型商贸企业，以进销存+基础财务为核心，不需要审批流程和扩展模块 |
| **Standard** | 同等功能规模，精简数据表设计，适合定制化开发基础 |
| **Full** | 中大型企业，需要完整的进销存+财务+CRM+HR+制造+项目管理全栈平台 |

---

## 升级路径

| 版本 | 规模（数据表 / 业务模块） | 说明 |
|------|--------------------------|------|
| Lite (简化版) | 62 表 / 6 业务模块（规划值） | 无审批/通知/HR/制造/报表 |
| Standard (标准版) | 72 表 / 6 业务模块（规划值） | 更精简的数据模型 |
| Full (完整版) | 227 表 <!-- stats:tables=227 --> / 23 业务模块 <!-- stats:modules=23 --> | 全面的企业平台能力 |

---

## 分支策略（2026-08-27 起）

> 适用于 `lite` / `standard` / `full` 三个版本分支，与 CI release 作业（幂等版本 tag）一致。
> **现状补充（2026-09-22 实测）**：三分支已删除，本节余下条目按「归档=提交与 tag」理解，
> 不再有可 checkout 的版本分支。

- **`main` 是唯一开发源**：所有功能开发、缺陷修复、依赖升级一律合入 `main`，提交由 Lead 统一执行。
- **版本分支仅归档、不维护**：`lite` / `standard` / `full` 冻结为历史归档分支，不再接收新提交、
  不再同步 `main` 增量，也不做强制更新或推送（避免维护三条代码线）；**冻结期结束后三分支已删除**，
  归档内容留存在 `main` 历史的 `eea90c0`。
- **版本差异以版本 tag 记录**：发版由 CI release 作业按最新 tag 幂等创建 `vX.Y.Z`
  （见 `scripts/bump-version.sh`）；各版本间的功能差异以 tag 及上文功能对比表为准，而非维护分支代码线。
- **验证**：`main` 的 CI 即版本发布验证，归档分支不再单独运行 CI。（2026-09-15 起 release 作业依赖为 `docs` + `e2e`；php 作业照跑但不拦发版——其红点是 CI 专属的集成测试历史债，见 `.github/workflows/ci.yml` 注释。）
