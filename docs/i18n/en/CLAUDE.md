# Open Admin Console (open-admin)

A full-stack admin console system based on webman v2 + Flutter.

![Octopus mascot](images/mascot.svg)

## Copyright Notice

```
Copyright (c) 2026 erik <erik@erik.xyz> — https://erik.xyz
```

> **Immutable, non-removable, irreversible.** All new files must include the above copyright notice as the file header comment.

## Ecosystem Roadmap

> Design spec: `superpowers/specs/2026-08-04-erp-ecosystem-roadmap-design.md`
> Architecture doc: `ARCHITECTURE.md` §21
> Feature matrix: `FUNCTIONS.md` §19

**Current overall score 89/100** — full roadmap P0~P3 completed, 22 modules full-stack coverage, production-ready.

| Phase | Duration | Deliverables | Status |
|------|------|--------|------|
| 🔵 **P0** Frontend ecosystem | 3-4 weeks | 97 Flutter pages + 34 HarmonyOS pages + 4 shared components | ✅ |
| 🟢 **P1** Business depth | 4-6 weeks | Finance engine + payroll engine + MRP + QMS + WebSocket | ✅ |
| 🟡 **P2** Operations reliability | 1-2 weeks | Migration rollback + auto backup + TraceId + dual queue drivers | ✅ |
| 🟣 **P3** Experience enhancement | 2-3 weeks | BI dashboards + EAM + multi-tenancy + DMS + 7 new tables | ✅ |

**Tests**: 513 tests, 2368 assertions (32 skipped) — ALL PASSING. **Flutter**: 0 errors, 0 warnings.

## Feature List

| Domain | Features |
|----|------|
| Authentication | Login/register/refresh/logout + captcha + account lockout + session limit |
| Dashboard | Business overview/sales board/inventory board/finance board (Redis 5m cache) |
| Users | CRUD + batch delete/enable-disable + Excel import |
| Roles & permissions | CRUD + permission tree + RBAC method.path authorization |
| System config | Key-value CRUD |
| Operation audit | Log queries + 8-platform source detection |
| Files | Upload + Excel/PDF export (sensitive data masked) |
| Security | 18-layer defense in depth (XSS/SQL injection/CSRF/rate limiting/CSP...) |
| Operations | Health check/Prometheus metrics/API docs/security.txt + Docker + CI/CD |
| Product management | Products/SKUs/categories/brands/warehouses/locations/suppliers/customers |
| Purchase management | Requisition→order→receiving→return→settlement (auto stock-in + AP generation) |
| Sales management | Quote→order→delivery→return→settlement (auto stock-out + AR generation) |
| Inventory management | Live inventory/flows/batches/transfers/counts/alerts (moving weighted average costing) |
| Finance management | AR-AP/vouchers/receipts-payments/journals/general ledger/subsidiary ledger/three statements/fixed assets/tax/multi-currency/budget |
| CRM | Opportunities/follow-ups/funnel/contacts/public pool/contracts/quotations/campaigns/tickets/analytics |
| Approval workflow | Workflow definitions/submit/approve/reject/withdraw/my approvals |
| Message notifications | Notification list/read/read-all/unread count |
| Project management | Projects/tasks/timesheets |
| Human resources | Departments/employees/positions/attendance/leave/payroll |
| Manufacturing | BOM/production orders/routings/workstations/MRP |
| Custom reports | Report templates/datasets/fields/filters/execution/scheduled dispatch |
| OMS order management | Multi-channel orders/fulfillment orchestration/inventory reservation (ATP)/RMA returns & exchanges/channel management |
| WMS warehouse management | Zones & locations (hierarchy+barcode)/inbound (ASN→receiving→putaway)/outbound (wave→pick→pack) |
| TMS transportation management | Carriers/freight rate comparison/shipments & labels/tracking events (webhook) |
| QMS quality management | Incoming IQC/in-process IPQC/outgoing OQC inspection + inspection standards + nonconformity handling |
| EAM equipment management | Equipment register/maintenance plans/repair orders/spare parts |
| DMS document management | Document categories/documents/version management |
| BI dashboards | Dashboard layouts/chart widgets |

## Tech Stack

### Backend
- PHP 8.3+, webman v2 (workerman/webman)
- Database: MySQL 8.0+, table prefix `erik_`
- Primary keys: BIGINT non-auto-increment, generated by `erikwang2013/snowflake-php`
- API-layer ID encryption: `erikwang2013/hashids`
- JWT authentication: `erikwang2013/jwt-webman`
- API sensitive data encryption: `erikwang2013/encryption`
- Database sensitive field encryption: `erikwang2013/encryptable`
- ES sync and query: `erikwang2013/webman-scout`
- Country flags: `erikwang2013/season`
- API doc generation: `hg/apidoc` | annotation-based, visit /apidoc

### Frontend
- Flutter 3.x, source directory `apps/flutter/`
- Web is designed in PC admin console style (not mobile app style)
- Supports both client and administrator ends
- HarmonyOS ArkTS, source directory `apps/harmonyos/`

## Project Structure

```
open-erp/
├── app/
│   ├── admin/controller/       # System management controllers (14)
│   │   ├── BaseController.php      # Base controller
│   │   ├── DashboardController.php # Dashboard + sales/inventory/finance panels
│   │   ├── UserController.php      # User CRUD + batch operations
│   │   ├── RoleController.php      # Role CRUD
│   │   ├── PermissionController.php# Permission CRUD
│   │   ├── ConfigController.php    # System config CRUD
│   │   ├── LogController.php       # Operation log queries
│   │   ├── ProfileController.php   # Profile center + logout
│   │   ├── ExportController.php    # Excel/PDF export
│   │   ├── ImportController.php    # Excel user import
│   │   ├── UploadController.php    # File upload
│   │   ├── HealthController.php    # Health check
│   │   ├── DocsController.php      # OpenAPI docs
│   │   └── MetricsController.php   # Prometheus metrics
│   ├── api/v1/controller/      # Client API (version-header controlled)
│   │   ├── CaptchaController.php   # Click captcha
│   │   ├── AuthController.php      # Login/register/refresh
│   │   └── ProductController.php   # Product queries (no purchase price)
│   ├── controller/              # Business module controllers (104, incl. InstallController)
│   │   ├── product/             # Products/categories/brands/warehouses/locations/suppliers/customers (7)
│   │   ├── purchase/            # Purchase requisitions/orders/receiving/returns/settlement (5)
│   │   ├── sales/               # Sales quotations/orders/deliveries/returns/settlement (5)
│   │   ├── inventory/           # Inventory/flows/transfers/counts/alerts (5)
│   │   ├── finance/             # AR-AP/vouchers/receipts-payments/journals/general ledger/subsidiary ledger/three statements/fixed assets/tax/multi-currency/budget/cost-profit centers (20)
│   │   ├── crm/                 # Opportunities/follow-ups/funnel/contacts/public pool/quotations/contracts/campaigns/tickets/analytics (10)
│   │   ├── workflow/            # Workflow definitions/approval submits/approvals/rejections/withdrawals (2)
│   │   ├── notification/        # Notification lists/read/unread counts (1)
│   │   ├── project/             # Projects/tasks/timesheets (3)
│   │   ├── hr/                  # Departments/employees/positions/attendance/leave/payroll (5)
│   │   ├── manufacturing/       # BOM/production orders/routings/workstations/MRP (5)
│   │   ├── report/              # Report templates/datasets/execution/scheduled dispatch (2)
│   │   ├── oms/                 # Orders/fulfillment/inventory reservation/RMA/channels (4)
│   │   ├── wms/                 # Zones & locations/ASN receiving/putaway/waves/picking/packing (8)
│   │   ├── tms/                 # Carriers/rates/shipments/labels/tracking (6)
│   │   ├── quality/             # IQC/IPQC/OQC/inspection standards/nonconformities (5)
│   │   ├── eam/                 # Equipment/maintenance plans/repair orders/spare parts (4)
│   │   ├── dms/                 # Document categories/documents/versions (2)
│   │   └── bi/                  # BI dashboards/chart widgets (3)
│   ├── service/                 # Business logic layer (container-registered, 24)
│   │   ├── finance/             # FinanceService: AR-AP auto-generation + receipt-payment reconciliation + journals
│   │   ├── inventory/           # InventoryService: stock in/out + moving weighted average costing
│   │   ├── notification/        # NotificationService: notification sending
│   │   └── oms/ wms/ tms/ quality/ hr/ manufacturing/  # Order/warehouse/transportation/QC/HR/manufacturing services
│   ├── common/                  # Shared utility classes (container-registered, 4)
│   │   ├── HashidsService.php   # ID encode/decode
│   │   ├── SnowflakeService.php # Snowflake ID generation
│   │   ├── EncryptionService.php# Data encryption + masking
│   │   └── I18n.php             # i18n translation
│   ├── middleware/              # Middleware (12)
│   │   ├── Locale.php           # Accept-Language auto-detection
│   │   ├── Cors.php             # Cross-origin
│   │   ├── SecurityFilter.php   # XSS/SQL injection/path traversal/command injection/CSRF blocking
│   │   ├── RateLimit.php        # Redis sliding-window rate limiting
│   │   ├── ApiVersion.php       # API version validation
│   │   ├── AdminAuth.php        # JWT authentication + blacklist
│   │   ├── AdminPermission.php  # RBAC permission validation
│   │   ├── OperationLog.php     # Auto operation-log recording
│   │   ├── TenantScope.php      # Multi-tenant isolation (static calls)
│   │   ├── TracingId.php        # End-to-end TraceId
│   │   ├── TrackingSignature.php# Request signature validation
│   │   └── StaticFile.php       # Static file serving (built into webman)
│   ├── model/                   # Data models (161)
│   ├── queue/                   # Queue tasks
│   └── process/                 # Processes (Http, Monitor)
├── apps/
│   ├── flutter/                 # Flutter all platforms (Web/iOS/Android/macOS/Windows/Linux)
│   │   └── lib/app/
│   │       ├── pages/           # Business pages (dashboard/login/user/role/config/log/profile + ERP)
│   │       ├── services/        # ApiService + AuthService + CaptchaService + ExportService
│   │       ├── layouts/        # Responsive layouts
│   │       └── theme/          # Material 3 theme
│   └── harmonyos/              # HarmonyOS client
├── config/                     # Config files
│   ├── route.php               # Routes + API version strategy
│   ├── middleware.php           # Global middleware registration
│   ├── translation.php          # Language config
│   └── plugin/hg/apidoc/        # API doc config (25 admin modules + 3 client modules)
├── database/
│   ├── install.sql              # Full install SQL (163 tables + seed data, all migrations merged)
│   ├── e2e-seed.sql             # E2E/CI minimal seed
│   └── backup/                 # Database backup scripts
│       ├── backup.sh           # mysqldump+gzip, 30-day retention
│       └── restore.sh          # Interactive restore
├── docs/                       # Docs
│   ├── ARCHITECTURE.md         # Mermaid architecture diagrams
│   ├── DESIGN.md               # Design document
│   ├── FEATURE_DESIGN.md       # Feature design document
│   ├── SECURITY.md             # Security architecture design
│   ├── API.md                  # API reference
│   ├── nginx-security.conf     # Nginx security reference config
│   ├── diagrams/               # Decomposed architecture diagrams
│   └── superpowers/            # Specs and plans
│       ├── specs/              # Design specs
│       └── plans/              # Implementation plans
├── public/                     # Public entry
├── runtime/                    # Runtime files
├── tests/                      # Tests
├── vendor/                     # Composer dependencies
├── CLAUDE.md                   # This file
├── README.md                   # Chinese readme
├── README_EN.md                # English readme
├── .env                        # Environment variables (not version-controlled)
├── .env.example                # Environment variable template
├── .env.docker                 # Docker environment variables
├── composer.json               # PHP dependencies
├── Dockerfile                  # Docker build (with OPcache + event + redis extensions)
├── docker-compose.yml          # Docker orchestration
└── .github/
    └── workflows/
        └── ci.yml              # CI/CD pipeline (PHP syntax + PHPStan + CS Fixer + PHPUnit + composer audit, multi-version matrix)
```

## Middleware Execution Chain

```
全局:  Locale → Cors → SecurityFilter(方法检查→405) → RateLimit → TracingId → {路由中间件}
/health:  Locale → Cors → SecurityFilter(方法检查→405) → RateLimit → TracingId → Controller
/install: Locale → Cors → SecurityFilter(方法检查→405) → RateLimit → TracingId → Controller
/admin:   Locale → Cors → SecurityFilter(方法检查→405) → RateLimit → TracingId → AdminAuth → AdminPermission → OperationLog → Controller
/api:     Locale → Cors → SecurityFilter(方法检查→405) → RateLimit → TracingId → ApiVersion → Controller
```

## Security Hardening

- **HTTP method restriction**: SecurityFilter only allows GET/POST/PUT/DELETE/OPTIONS/HEAD; non-standard methods return 405
- **CSP headers**: Content-Security-Policy + X-Permitted-Cross-Domain-Policies injected into all responses
- **Account lockout**: 5 consecutive failed logins lock the account for 15 minutes
- **Concurrent session limit**: at most 3 valid Tokens per user; the oldest Token is blacklisted when exceeded
- **security.txt**: `/.well-known/security.txt` RFC 9116 endpoint
- **Nginx security config**: `nginx-security.conf` reverse-proxy security hardening reference

## API Version Strategy

Versions are controlled by the `API-Version` request header (default `v1`), not reflected in the URL:

```bash
curl -H "API-Version: v1" http://localhost:8787/api/auth/login
```

Adding a version only requires creating the `app/api/{version}/controller/` directory and registering it in the `ApiVersion` middleware.

## Rate Limiting Strategy

Redis sliding window (Lua atomic), default 60 times/minute/IP/route:
- Login: 10 times/minute
- Registration: 5 times/minute
- Response headers: `X-RateLimit-Limit/Remaining/Reset`, plus `Retry-After` when over limit

## Code Standards

### PHP
- No leading `\` for global functions/class references; use `use` imports
- Config files must include Chinese comments explaining the meaning of each config item
- All new `.php` files must start with the copyright notice

### Database
- Table prefix: `erik_`
- Primary key `id`: BIGINT type, non-auto-increment, generated by snowflake
- Sensitive fields auto-encrypt/decrypt via the `erikwang2013/encryptable` trait
- schema uses database/install.sql as its single source of truth (single-file SQL)

### Flutter
- Web layout uses PC admin console style (sidebar + top bar + content area)
- GetX state management, `ApiService` singleton (Dio + JWT interceptor)
- Token persistence via `shared_preferences`
- Responsive breakpoints: mobile (< 768px) and desktop (>= 768px)

### HarmonyOS
- Native HTTP client via `@ohos.net.http`
- Token silent refresh: auto-calls `/api/auth/refresh` on 401
- Refresh failure auto-redirects to the login page

## Deployment

### Docker Compose (recommended for production)

The project root `docker-compose.yml` orchestrates 5 services:

| Service | Description |
|------|------|
| `nginx` | Nginx reverse proxy (80/443), static file serving |
| `app` | webman PHP 8.3 app, built via `Dockerfile` (with OPcache + event + redis) |
| `mysql` | MySQL 8.0, data volume persistence |
| `redis` | Redis 7 Alpine, cache/rate limiting/sessions |
| `elasticsearch` | Elasticsearch 8.x, full-text search |

```bash
cp .env.docker .env
docker-compose up -d
```

### CI/CD

`.github/workflows/ci.yml` defines the GitHub Actions pipeline (PHP 8.2/8.3/8.4 matrix):

- PHP syntax check (`php -l`)
- PHPStan static analysis (`vendor/bin/phpstan analyse`)
- PHP CS Fixer style check (`vendor/bin/php-cs-fixer fix --dry-run --diff`)
- PHPUnit unit tests
- Composer security audit (`composer audit --no-dev`)

### Database Backup

`database/backup/backup.sh` — mysqldump + gzip, auto-cleans backups older than 30 days.
`database/backup/restore.sh` — interactive restore, lists available backups to choose from.

### Monitoring

The `GET /metrics` endpoint (`MetricsController`) outputs Prometheus text format with 5 gauge metrics:
- `openadmin_http_requests_total` — total request count
- `openadmin_active_users` — active user count
- `openadmin_db_connection_status` — database connection status (0/1)
- `openadmin_redis_connection_status` — Redis connection status (0/1)
- `openadmin_memory_usage_bytes` — memory usage
