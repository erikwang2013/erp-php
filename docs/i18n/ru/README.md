# Открытая ERP-система (open-erp)

Полноценная ERP-система на webman v2 + Flutter.

<div align="center"><img src="images/mascot.svg" alt="open-erp талисман-осьминог Сяо-Бачжао" width="150"></div>

<div align="center">🌐 [中文](../../../README.md) | [English](../en/README.md) | [한국어](../ko/README.md) | Русский | [Deutsch](../de/README.md) | [Français](../fr/README.md) | [Español](../es/README.md) | [Português](../pt/README.md) | [हिन्दी](../hi/README.md) | [العربية](../ar/README.md) | [বাংলা](../bn/README.md) | [Bahasa Indonesia](../id/README.md) | [日本語](../ja/README.md)</div>

> [English version](../en/README.md) | [Сравнение версий](EDITIONS.md) | [Диаграммы архитектуры](ARCHITECTURE.md) | [Системная архитектура](#системная-архитектура) | [Дизайн-документ](DESIGN.md) | [Безопасность](SECURITY.md) | [Справочник API](API.md) | [Руководство по функциям](FUNCTIONS.md)

## Перечень функций

| Бизнес-домен | Функция | Описание |
|--------|------|------|
| 🔐 Аутентификация | Вход/регистрация/обновление токена/выход | Капча по клику + JWT + чёрный список |
| | Блокировка аккаунта | 5 неудачных попыток — блокировка на 15 минут |
| | Ограничение параллельных сессий | Не более 3 действующих токенов на пользователя |
| 📊 Дашборд | Обзор бизнеса/продажи/склад/финансы | Кэш Redis 5 минут |
| 👥 Управление пользователями | CRUD + массовое удаление/включение-отключение | Мягкое удаление + повторное подтверждение паролем |
| | Массовый импорт Excel | Построчная проверка + отчёт об ошибках |
| 🔒 Роли и права | CRUD ролей + дерево прав | RBAC-авторизация с точностью method.path |
| ⚙ Системная конфигурация | CRUD пар ключ-значение | Групповое управление |
| 📋 Аудит операций | Просмотр логов + определение источника | Автоопределение 8 платформ |
| 📁 Управление файлами | Загрузка/экспорт Excel/экспорт PDF | Автоматическое маскирование чувствительных данных |
| 🛡 Защита | 18 уровней эшелонированной обороны | XSS/SQL-инъекции/обход путей/инъекции команд/CSRF/лимит запросов/CSP... |
| 🏥 Эксплуатация | Проверка работоспособности/metrics/документация API/security.txt | Prometheus + OpenAPI 3.0 |
| 📦 Управление товарами | Карточки товаров/SKU/множественные спецификации/единицы/категории/бренды/ценовые стратегии | Многоуровневое дерево категорий + пересчёт единиц |
| | Склады и ячейки | Управление несколькими складами и ячейками |
| | Карточки поставщиков/клиентов | Контакты/банковские счета/кредитные лимиты |
| 📥 Закупки | Заявка→заказ→приёмка→возврат→расчёт | Полный цикл закупок + согласование |
| 📤 Продажи | Предложение→заказ→отгрузка→возврат→расчёт | Предложение в заказ + валовая прибыль |
| | Кредитный контроль клиента | Управление лимитом/сроком оплаты/заморозкой + блокировка превышения лимита и просрочки по заказам/отгрузкам |
| 🏗 Управление складом | Реальные остатки/партии/серийные номера/перемещение/инвентаризация/предупреждения | Расчёт себестоимости по скользящему средневзвешенному |
| 💰 Финансы |
| | Мультиорганизация + консолидированная отчётность | Учёт нескольких компаний и наборов счетов + элиминирующие проводки (метод долевого участия/по стоимости) |
| | Учёт себестоимости запасов и производства | Отпуск материалов → сбор трудозатрат/накладных расходов → себестоимость готовой продукции → списание отклонений |
| | Векселя + сверка с банком | Реестр векселей + автозачёт по импортированным банковским выпискам |
| | Пул входящих счетов-фактур + электронные счета-фактуры | Управление входящими счетами-фактурами + выставление (адаптер + Mock-канал) | Дебиторка/кредиторка/платежи/журнал/расходы/отчёт о прибыли/основные средства/налоги/мультивалюта/бюджет/центры затрат и прибыли | Автогенерация дебиторки/кредиторки + зачёт + полное финансовое управление |
| 🤝 CRM | Клиенты/контакты/история контактов/кампании/сервисные тикеты/аналитика/воронка продаж/общий пул/предложения/договоры | Управление полным жизненным циклом клиента |
| | Движок ценности клиентов | Операции с предоплатой/баллами/картами и купонами |
| ✅ Workflow согласования | Определение workflow/отправка на согласование/утверждение/отклонение/отзыв/мои согласования | Многоузловой движок процессов |
| | Визуальный конструктор workflow | Конфигурация узлов/ветвей/рёбер возврата на холсте, переиспользование движка согласования |
| 🔔 Уведомления | Список/отметка о прочтении/счётчик непрочитанных/все прочитаны | Реальная рассылка и отслеживание статусов |
| | Многоканальные уведомления | Драйверы каналов SMS/email (Mock-канал + лог + повторы) |
| 📐 Управление проектами | Проекты/задачи/учёт трудозатрат | Отслеживание прогресса и управление ресурсами |
| | Затраты и бюджет проекта | Трудозатраты × ставка → накопление затрат по проекту + отклонение от бюджета |
| 👤 HR | Отделы/сотрудники/должности/посещаемость/отпуска/зарплаты | Полное управление персоналом |
| | Подбор/оценка/обучение/соцстрахование | Воронка найма + оценка KPI/360 + учебные кредиты + правила базы взносов и расчётный листок |
| 🏭 Производство | BOM/производственные заказы/технологические маршруты/рабочие станции/MRP | Планирование потребности в материалах и исполнение |
| | Отчёты об операциях/сдельная оплата/закрытие субподряда | Уровень исполнения операций MES + отпуск материалов и закрытие по субподрядным заказам |
| | Анализ загрузки мощностей | Календарь рабочих центров + отчёт по укрупнённой загрузке мощностей |
| | Прослеживаемость партий/серийных номеров | Прямая и обратная цепочка трассировки + предупреждение о приближении срока годности |
| 📈 Пользовательские отчёты | Шаблоны/наборы данных/поля/фильтры/выполнение/расписание | Визуальный конструктор отчётов |
| 📋 OMS (управление заказами) | Многоканальные заказы/оркестрация исполнения/резервирование остатков/распределение/отмена/RMA | Управление полным жизненным циклом заказа |
| 🏗 WMS (управление складом) | Зоны и ячейки/ASN/приёмка/размещение/волны/комплектация/упаковка/отгрузка | Полный складской процесс |
| 🚚 TMS (транспорт) | Перевозчики/услуги/тарифы/накладные/трекинг/счета за фрахт | Сравнение тарифов перевозчиков + трекинг |
| 🛠 EAM (управление оборудованием) | Реестр оборудования/планы обслуживания/ремонтные заказы/запчасти | Управление полным жизненным циклом оборудования |
| | Осмотр по QR-коду | Скан-осмотр, при отклонениях — автоматическое создание ремонтного заказа |
| 🌐 Платформа и открытость | Версионирование путей API | Панель управления /admin/v1, клиент /api/v1, открытый /open/v1 (без заголовка версии) |
| | Движок шаблонов печати документов | Рендеринг плейсхолдеров + PDF через dompdf + QR-этикетки |
| | Пользовательские поля форм | Расширение мастер-таблиц через custom_fields JSON + валидация |
| | Мультитенантная архитектура | Тенант erp_tenant + контекст запроса TenantScope + оплата по истечении срока (seam middleware зарезервирован, не зарегистрирован) |

## Модули ERP

Потоки данных между бизнес-модулями:

- Приёмка закупок → автоматическое оприходование (скользящее средневзвешенное) → автоматическая кредиторка
- Отгрузка продаж → автоматическое списание → автоматическая дебиторка
- Платежи → зачёт дебиторки/кредиторки → обновление журнала
- Проводки → автоматическое обновление Главной книги (по счетам) + вспомогательной книги (по операциям)
- Балансовый отчёт → автосводка исходящих остатков Главной книги
- Отчёт о движении денежных средств → автосводка кассового/банковского журнала (операционная/инвестиционная/финансовая деятельность)
- Workflow → бизнес-документы на согласование → многоузловое движение → результат согласования возвращается в модуль
- Уведомления → согласования/предупреждения/события системы → реальная рассылка → отметка о прочтении
- MRP → на основе заказов продаж + BOM → расчёт потребности в материалах → закупочные/производственные рекомендации
- OMS → импорт многоканальных заказов → резервирование остатков (ATP) → создание исполнения → передача в WMS на комплектацию/упаковку
- WMS → агрегация волн → задачи комплектации → подтверждение → завершение упаковки → генерация накладной TMS
- TMS → сравнение тарифов → создание накладной → подтверждение отгрузки (stockOut+AR) → трекинг → получение
- Поступление в WMS → ASN → приёмка → контроль качества → подтверждение размещения (stockIn+AP) → обновление остатков
- RMA → заявка на возврат → согласование → возврат на склад → возврат средств

## Технологический стек

| Слой | Технология | Описание |
|---|------|------|
| Бэкенд-фреймворк | webman v2 (workerman) | Высокопроизводительный PHP-фреймворк с резидентными процессами |
| Версия PHP | 8.3+ | |
| База данных | MySQL 8.0+ | Префикс таблиц `erp_`, BIGINT-ключи без AUTO_INCREMENT |
| Поиск | Elasticsearch | Синхронизация и поиск через `webman-scout` |
| Веб-интерфейс админки | Flutter 3.x | PC-стиль админки (`apps/flutter/`) |
| Мобильный клиент | HarmonyOS ArkTS | Нативный клиент HarmonyOS (`apps/harmonyos/`), телефоны/планшеты/2in1 |

## Ключевые зависимости

| Пакет | Назначение |
|---|------|
| `erikwang2013/snowflake-php` | Генерация глобально уникальных BIGINT-ключей по алгоритму Snowflake |
| `erikwang2013/hashids` | Шифрование/дешифрование ID на уровне API, скрытие реальных ID в БД |
| `erikwang2013/jwt-webman` | Выпуск и проверка JWT-токенов |
| `erikwang2013/encryption` | Шифрование чувствительных данных на транспортном уровне |
| `erikwang2013/encryptable` | Автошифрование чувствительных полей на уровне хранения |
| `erikwang2013/webman-scout` | Синхронизация и полнотекстовый поиск в Elasticsearch |
| `erikwang2013/season` | Данные государственных флагов |
| `erikwang2013/poster-php` | Капча по клику + генерация постеров |
| `erikwang2013/security-php` | Проверки инструментов безопасности |
| `phpoffice/phpspreadsheet` | Экспорт Excel |
| `barryvdh/laravel-dompdf` | Экспорт PDF (на базе Dompdf) |
| `hg/apidoc` | Автогенерация документации API | Аннотационная документация, группы админка/клиент |

## Интернационализация

Интернационализация | Автоопределение по заголовку Accept-Language | Поддержка китайского/английского языков

## Структура проекта

```
open-erp/
├── app/
│   ├── admin/controller/       # Контроллеры системного управления (14)
│   ├── api/v1/controller/      # Клиентское API (версия из заголовка API-Version)
│   ├── controller/             # Контроллеры бизнес-модулей (88)
│   │   ├── product/            # Товары/категории/бренды/склады/ячейки/поставщики/клиенты (7)
│   │   ├── purchase/           # Заявки/заказы/приёмка/возврат/расчёт закупок (5)
│   │   ├── sales/              # Предложения/заказы/отгрузка/возврат/расчёт продаж (5)
│   │   ├── inventory/          # Остатки/операции/перемещение/инвентаризация/предупреждения (5)
│   │   ├── finance/            # Дебиторка/кредиторка/проводки/платежи/журнал/главная книга/вспом. книга/отчёты/активы/налоги/валюта/бюджет/центры (20)
│   │   ├── crm/                # Сделки/контакты/воронка/контакты/пул/договоры/предложения/маркетинг/тикеты/аналитика (10)
│   │   ├── workflow/           # Определения workflow/отправка/утверждение/отклонение/отзыв (2)
│   │   ├── notification/       # Список уведомлений/прочитано/непрочитано (1)
│   │   ├── project/            # Проекты/задачи/трудозатраты (3)
│   │   ├── hr/                 # Отделы/сотрудники/должности/посещаемость/отпуска/зарплаты (5)
│   │   ├── manufacturing/      # BOM/производственные заказы/маршруты/станции/MRP (5)
│   │   ├── report/             # Шаблоны/наборы данных/выполнение/расписание (2)
│   │   ├── oms/                # OMS-заказы/исполнение/RMA/каналы (4)
│   │   ├── wms/                # Зоны/ячейки/ASN/приёмка/размещение/волны/комплектация/упаковка (8)
│   │   └── tms/                # Перевозчики/услуги/тарифы/накладные/трекинг/счета (6)
│   ├── service/                # Слой бизнес-логики
│   │   ├── inventory/          # Приход/расход + скользящее средневзвешенное + резервирование/ATP
│   │   ├── finance/            # Автогенерация дебиторки/кредиторки + зачёт
│   │   ├── notification/       # Сервис отправки уведомлений
│   │   ├── oms/                # Оркестрация заказов/распределение остатков/жизненный цикл RMA
│   │   ├── wms/                # Поступление (ASN→приёмка→размещение) / отгрузка (волна→комплектация→упаковка)
│   │   └── tms/                # Накладные/сравнение тарифов/трекинг
│   ├── model/                  # 161 модель Eloquent (общие для модулей)
│   ├── middleware/             # 12 промежуточных слоёв
│   ├── common/                 # Сервисы Hashids/Snowflake/Encryption
│   └── queue/                  # Задачи очереди
├── apps/
│   ├── flutter/                # Flutter кроссплатформенно (Web PC + iOS/Android/macOS/Windows/Linux)
│   └── harmonyos/              # Нативный клиент HarmonyOS
├── config/                     # Конфигурационные файлы (с комментариями на китайском)
│   ├── plugin/hg/apidoc/        # Конфигурация документации API
├── database/
│   ├── install.sql              # Полный установочный SQL (163 таблицы + seed-данные)
│   ├── e2e-seed.sql             # Минимальный seed для E2E/CI
│   └── backup/                 # Скрипты резервного копирования/восстановления
├── docs/                       # Документация по архитектуре, дизайну, безопасности, API
├── tests/                      # PHPUnit-тесты (20 файлов, 137 методов, 805 утверждений)
├── resource/
│   └── translations/           # Файлы переводов (zh_CN, en)
│       ├── zh_CN/              # Китайские переводы (127 ключей)
│       └── en/                 # Английские переводы (127 ключей)
├── public/                     # Публичная точка входа
├── runtime/                    # Файлы времени выполнения
└── vendor/                     # Зависимости Composer
```

## Системная архитектура

> Нажмите на изображение, чтобы увидеть оригинальный SVG. Диаграммы используют английские названия и полно показывают архитектуру системы на всех уровнях.

### Топология системы

![System Architecture](diagrams/system-architecture-cn.svg)

**Пятиуровневая архитектура**: клиентский слой → шлюз/пограничный слой (обратный прокси Nginx) → прикладной слой (webman v2 + цепочка middleware + аутентификация/авторизация + бизнес-логика + общие сервисы) → слой хранения (MySQL + Redis + Elasticsearch) → эксплуатационный слой (CI/CD + Docker + Prometheus)

### Бизнес-процессы

![Business Flowchart](diagrams/business-flowchart-cn.svg)

**Связка семи бизнес-доменов**: закупки → склад → продажи → финансы образуют ядро цепочки поставок; CRM управляет продажами; производственный MRP на основе заказов продаж + BOM формирует планы закупок и производства; workflow согласования, уведомления, проекты, HR — поддерживающие модули на всём пути.

### Обзор функциональных модулей

![Functional Modules](diagrams/functional-modules-cn.svg)

**19 бизнес-доменов, 163 таблицы, 121 контроллер**: аутентификация/безопасность, дашборд, системное управление, защита, эксплуатация/мониторинг, товары, закупки, продажи, склад, финансы (14 подмодулей), CRM (10 подмодулей), workflow согласования, уведомления, проекты, HR, производство (MRP), пользовательские отчёты, OMS, WMS, TMS, QMS, EAM, DMS, BI-панели.

### Жизненный цикл запроса

![Request Lifecycle](diagrams/request-lifecycle-cn.svg)

**Полный путь запроса от клиента до БД**: клиент (Flutter/HarmonyOS) → Nginx (termination SSL) → определение языка → CORS → фильтр безопасности → лимит запросов → проверка версии API → [админка: JWT-аутентификация → RBAC → журнал операций] → контроллер → слой сервисов → слой моделей → кэш/БД/поиск → JSON-ответ. На диаграмме показаны пути попадания и промаха кэша.

### Эшелонированная оборона

![Security Architecture](diagrams/security-architecture-cn.svg)

**18 уровней обороны**: L0 физическая сеть → L1 безопасность транспорта → L2 HTTP-заголовки безопасности → L3 проверка запросов → L4 очистка ввода → L5 защита CSRF → L6 лимит запросов → L7 аутентификация (JWT+Captcha+чёрный список+контроль сессий) → L8 авторизация RBAC → L9 защита данных (шифрование транспорта+хранения+обфускация ID+маскирование) → L10 аудит/мониторинг → L11 раскрытие для соответствия.

---

## Требования к окружению

- PHP >= 8.3
- Composer 2.x
- MySQL >= 8.0
- Flutter >= 3.41 (только для разработки фронтенда)
- Elasticsearch >= 7.x (опционально, для поиска)

## Быстрый старт

### 1. Установка зависимостей

```bash
composer install
```

### 2. Настройка переменных окружения

Скопируйте и измените переменные окружения (опционально; без настройки используются значения по умолчанию из `config/*.php`):

```bash
cp .env.example .env
```

Ключевые параметры:

| Переменная окружения | Описание | По умолчанию |
|---------|------|--------|
| `JWT_SECRET` | Ключ подписи JWT | `open-admin-jwt-secret-change-in-production` |
| `HASHIDS_SALT` | Соль Hashids | `open-admin-hashids-salt-2026` |
| `ENCRYPTION_KEY` | Ключ шифрования API | 32-байтовое значение по умолчанию |
| `SNOWFLAKE_DATACENTER_ID` | ID дата-центра (0-31) | `1` |
| `SNOWFLAKE_WORKER_ID` | ID рабочего узла (0-31) | `1` |
| `SCOUT_HOSTS` | Адрес ES | `http://localhost:9200` |

**В продакшене обязательно замените все ключи на случайные строки.**

### 3. Инициализация базы данных

**Способ 1: веб-мастер установки (рекомендуется)**

После запуска откройте `http://localhost:8788/install` и пройдите установку из 4 шагов: проверка окружения → настройка БД → аккаунт администратора → установка в один клик.

**Способ 2: импорт из командной строки**

```bash
mysql -u root -p имя_базы < database/install.sql
```

`install.sql` объединяет 29 файлов миграций и содержит все 163 таблицы и seed-данные.

**Способ 3: окружение Docker**

```bash
```

### 4. Запуск сервиса

```bash
php start.php start
```

По умолчанию слушает `http://0.0.0.0:8788`.

### 5. Запуск фронтенда (опционально)

**Flutter-админка (Web):**

```bash
cd apps/flutter
flutter pub get
flutter run -d chrome    # Web (PC-стиль админки)
```

**Клиент HarmonyOS (мобильный):**

Откройте каталог `apps/harmonyos/` в DevEco Studio и запустите на реальном устройстве или эмуляторе.

### 6. Развёртывание Docker Compose одним кликом (рекомендуется для продакшена)

Проект содержит полную оркестрацию Docker из 5 сервисов: Nginx, PHP (webman app), MySQL, Redis, Elasticsearch.

```bash
# 1. Настройка переменных окружения Docker
cp .env.docker .env
# 2. Заменить ключи-заглушки случайными значениями (idempotent)
bash scripts/gen-env-keys.sh .env

# 3. Запуск всех сервисов
docker compose up -d

# 4. Инициализация БД (выполнить внутри контейнера app)

# 5. Доступ
# http://localhost:8788  (webman)
# http://localhost:8080  (обратный прокси Nginx)
```

- `Dockerfile`: PHP 8.3 + OPcache + Composer, на основе `php:8.3-cli`
- `docker-compose.yml`: оркестрация 5 сервисов, изоляция сети, персистентные тома
- `.env.docker`: переменные окружения для Docker

## Использование

### 1. Вход

При первом использовании откройте веб-установщик `http://localhost:8788/install`, чтобы завершить установку и создать учётную запись администратора. Если система уже установлена, откройте консоль, введите учётные данные и пройдите капчу с кликами для входа.

### 2. Навигация

После входа модули доступны через боковое меню: панель управления, товары, закупки, продажи, склад, финансы, CRM, workflow согласования, уведомления, проекты, персонал, производство, пользовательские отчёты, OMS/WMS/TMS, BI-панели и администрирование системы (пользователи/роли/конфигурация/журналы). На десктопе меню зафиксировано, на мобильных сворачивается в выдвижную панель.

### 3. Права и безопасность

- Функции и API управляются через RBAC; меню и интерфейсы без прав недоступны (403)
- Чувствительные операции (удаление пользователей/ролей) требуют повторного ввода текущего пароля в теле запроса
- После выхода токен немедленно попадает в чёрный список

### 4. Мультиязычность

Автоматическое переключение по заголовку `Accept-Language` (zh-CN / en), по умолчанию китайский.

## Стандарты базы данных

- **Префикс таблиц**: `erp_`
- **Первичные ключи**: все таблицы используют `id BIGINT UNSIGNED NOT NULL`, **AUTO_INCREMENT запрещён**
- **Генерация ID**: ключи генерируются на прикладном уровне через `SnowflakeService::generate()`, распределённо уникальны
- **Обязательные поля**: каждая таблица должна содержать `id`, `created_at`, `updated_at`
- **Мягкое удаление**: таблицам с мягким удалением добавляется `deleted_at DATETIME DEFAULT NULL`
- **Чувствительные поля**: телефон, email, ID-номер и т.п. автоматически шифруются/дешифруются плагином `encryptable`; в БД хранятся как `VARCHAR(500)` шифротекст

## Стандарты API

### Документация API

Проект автоматически генерирует документацию через hg/apidoc — доступно по адресу `/apidoc`.

- Админка (Admin): 25 групп модулей, полные параметры запросов и структуры ответов
- Клиентское API (Service API): 3 группы — аутентификация/капча/товары
- Все эндпоинты аннотированы глобальными заголовками: JWT-аутентификация, версия API, интернационализация

### Единый формат ответа

```json
{
    "code": 0,
    "message": "success",
    "data": {}
}
```

### Коды ошибок бизнеса

| Код | Значение | Описание |
|-------|------|------|
| `0` | Успех | |
| `400` | Ошибка параметров запроса | |
| `401` | Не авторизован (недействительный или истёкший токен) | |
| `403` | Нет прав / блокировка безопасности | Ошибка RBAC / обнаружение атак SecurityFilter |
| `404` | Ресурс не найден | |
| `422` | Ошибка валидации параметров | |
| `413` | Тело запроса слишком велико | SecurityFilter, более 10MB |
| `405` | Метод не разрешён | SecurityFilter, разрешены только GET/POST/PUT/DELETE/OPTIONS/HEAD |
| `415` | Неподдерживаемый тип носителя | SecurityFilter, Content-Type не JSON |
| `429` | Слишком много запросов | Сработал RateLimit / блокировка аккаунта (5 неудачных входов — блок на 15 минут) |
| `500` | Внутренняя ошибка сервера | |

### Интернационализация

Заголовок `Accept-Language` автоматически переключает язык (zh-CN → китайский, en → English), по умолчанию китайский.

### Обработка ID

- **ID в запросах/ответах**: шифруются hashids в строки, реальные ID БД не раскрываются
- **Пути эндпоинтов**: `GET /admin/user/{hashid}` — `{id}` в пути это hashid-строка
- **Хранение в БД**: исходное значение BIGINT, сгенерировано snowflake

### Версия API

Версия API управляется через заголовок запроса, **не отражается в URL**:

```http
API-Version: v1
```

- Без заголовка по умолчанию используется `v1`
- Неподдерживаемая версия возвращает `400 Bad Request`
- Для новой версии достаточно создать каталог `app/api/{version}/controller/` и зарегистрировать версию в middleware

### Лимит запросов

Алгоритм скользящего окна на Redis, по умолчанию 60 запросов/мин/IP/маршрут. Чувствительные эндпоинты строже:
- Вход: 10 запросов/мин
- Регистрация: 5 запросов/мин (по умолчанию выключено, включить через `REGISTRATION_ENABLED=1`)

Ответ содержит заголовки `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`. Превышение возвращает 429 с заголовком `Retry-After`.

### Архитектура middleware

Глобальные middleware применяются ко всем запросам по порядку:

```
Locale (автоопределение Accept-Language, установка локали)
  → Cors (предобработка CORS + заголовки ответа)
  → SecurityFilter (ограничение методов/размер тела/проверка Content-Type/XSS/SQL-инъекции/обход путей/инъекции команд/CSRF)
  → RateLimit (скользящее окно Redis + блокировка аккаунта: 5 неудач — блок 15 минут)
  → ApiVersion (проверка версии API, группа маршрутов /api)
  → AdminAuth (JWT-аутентификация + чёрный список, группа /admin)
  → AdminPermission (RBAC, группа /admin)
  → OperationLog (автозапись POST/PUT/DELETE с определением источника, группа /admin)
```

`/health`, `/api/docs` и `/install` — публичные эндпоинты, проходят только `Locale → Cors → SecurityFilter → RateLimit`.

Усиление безопасности:
- **Блокировка аккаунта**: 5 неудачных входов подряд — аккаунт блокируется на 15 минут, вход возвращает 429
- **Ограничение параллельных сессий**: не более 3 действующих токенов на пользователя; при превышении самый старый токен автоматически попадает в чёрный список
- **security.txt**: `GET /.well-known/security.txt` предоставляет контактную информацию по стандарту RFC 9116
- **Конфигурация Nginx**: пример усиления обратного прокси — `docs/nginx-security.conf`

### Аутентификация

Вход и регистрация требуют проверки **капчи по клику**:

1. Клиент запрашивает `POST /api/captcha/generate` и получает изображение капчи (base64 PNG) и список целей
2. Пользователь кликает по соответствующим местам на изображении в указанном порядке, собираются координаты кликов `[{x, y}, ...]`
3. При входе отправляются `captcha_key` и `clicks`; сервер сначала проверяет капчу, затем учётные данные

```http
POST /api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "******",
  "captcha_key": "abc123...",
  "clicks": [{"x": 120, "y": 85}, {"x": 210, "y": 140}, {"x": 95, "y": 170}]
}
```

Дальнейшие запросы админки требуют JWT:

```http
Authorization: Bearer <token>
```

После успешного входа возвращается access_token (срок 2 часа) и refresh_token (срок 14 дней).

При выходе токен попадает в чёрный список Redis и не может использоваться до истечения срока. POST /admin/profile/logout

### Повторное подтверждение чувствительных операций

Чувствительные операции (удаление пользователя, роли, прав и т.п.) требуют передачи `password` текущего пользователя в теле запроса для повторного подтверждения:

```http
DELETE /admin/user/{id}
Content-Type: application/json
Authorization: Bearer <token>

{ "password": "******" }
```

## Список API

Полный список эндпоинтов (публичные / админка / бизнес / клиентские) вынесен в отдельный документ:

→ [Справочник API](API.md)

## О фронтенде

### Flutter-админка (PC-стиль)

- **Макет**: боковая панель (сворачиваемая 64px/240px) + верхняя панель + область контента, три адаптивные точки (телефон/планшет/десктоп)
- **Страницы**: вход, дашборд, пользователи, роли и права, системная конфигурация, журнал операций, личный кабинет
- **Управление состоянием**: GetX (`ApiService` синглтон + `AuthService` с персистентным токеном)
- **Дашборд**: карточки статистики, графики трендов (fl_chart), круговые диаграммы, последние операции
- **Экспорт**: Excel/PDF, PDF содержит неудаляемую информацию об авторских правах
- **Массовые операции**: множественное удаление, массовое включение/отключение
- **Тема**: Material 3, светлая/тёмная

### Мобильный клиент HarmonyOS

- **Страницы**: вход, дашборд, список/детали пользователей, личный кабинет
- **Аутентификация**: JWT Bearer + автоматическое бесшовное обновление токена при 401, при неудаче — редирект на страницу входа
- **Хранение**: токен через AppStorage

## Стандарты разработки

- Глобальные функции/классы ссылаются без префикса `\`, используется импорт через `use`
- Все PHP-файлы должны содержать объявление авторских прав в начале
- Все конфигурационные файлы должны содержать комментарии на китайском
- Первичные ключи БД должны генерироваться snowflake на прикладном уровне, автоинкремент запрещён
- Все ID в параметрах и ответах API должны шифроваться/дешифроваться через hashids
- Middleware AdminPermission кэширует права пользователя в Redis (TTL=60s), устраняя узкое место N+1

## Развёртывание

### Docker Compose (рекомендуется)

В корне проекта `docker-compose.yml` — оркестрация 5 сервисов:

| Сервис | Образ | Порт |
|------|------|------|
| `nginx` | nginx:alpine | 80, 443 |
| `app` | локальная сборка `Dockerfile` | 8788 |
| `mysql` | mysql:8.0 | 3306 |
| `redis` | redis:7-alpine | 6379 |
| `elasticsearch` | elasticsearch:8.x | 9200 |

PHP-образ собирается через `Dockerfile` на базе `php:8.3-cli` с включённым OPcache.

```bash
cp .env.docker .env
# Заменить ключи-заглушки случайными значениями (idempotent)
bash scripts/gen-env-keys.sh .env
docker compose up -d
```

### CI/CD

Конвейер непрерывной интеграции GitHub Actions: `.github/workflows/ci.yml`

- Проверка синтаксиса PHP (`php -l`)
- Модульные тесты PHPUnit
- Статический анализ Flutter (`flutter analyze`, включён в CI — см. flutter job в `.github/workflows/ci.yml`)

### Резервное копирование БД

Каталог `database/backup/`:

- `backup.sh` — резервное копирование mysqldump + gzip, автоочистка резервных копий старше 30 дней
- `restore.sh` — интерактивное восстановление, список доступных копий на выбор

### Конфигурация безопасности Nginx

Для продакшена используйте `docs/nginx-security.conf` для усиления обратного прокси.

## Открытый исходный код — поддержите нас

| WeChat | Alipay |
|:---:|:---:|
| ![WeChat](images/weixinpay.png "WeChat") | ![Alipay](images/alipay.png "Alipay") |

### Международный перевод (банковский / Global Bank Transfer)

**Информация получателя**

- Имя получателя: WANG KEXUN
- Номер счёта получателя: 881015918251

**Банк получателя**

- SWIFT-код ZA Bank: AABLHKHHXXX
- Название банка: ZA Bank Limited
- Банковский код: 387
- Адрес банка: Core F, Cyberport 3, 100 Cyberport Road, Hong Kong

**Банк-корреспондент для трансграничных переводов (при необходимости)**

> Это банк-корреспондент (промежуточный банк), а не банк получателя. Уточните в своём банке, требуется ли его указывать.

- Для гонконгских долларов, юаней и долларов США: Citibank N.A. Hong Kong — SWIFT `CITIHKHXXXX`, банковский код 006, отделение Hong Kong Branch, код отделения 391, Citibank Tower, Citibank Plaza, 3 Garden Road, Central, Hong Kong
- Для других валют: THE BANK OF NEW YORK MELLON — SWIFT `IRVTUS3NXXX`, 240 GREENWICH STREET, NEW YORK, United States

### Пожертвование в криптовалюте (Crypto Donation)

Если этот проект помог вам, отсканируйте QR-код, чтобы сделать пожертвование, спасибо!

| <img src="../../coin/1.jpg" width="200" alt="BNB Smart Chain (BEP20)"><br>**BNB Smart Chain (BEP20)**<br>`0x355d429f97511897ccb4e271ec888205f9ab6629` | <img src="../../coin/2.jpg" width="200" alt="Tron (TRC20)"><br>**Tron (TRC20)**<br>`TEdDHWLajt1XvqtPDWmQctdrJaC3pzZZzz` |
| <img src="../../coin/3.jpg" width="200" alt="Ethereum (ERC20)"><br>**Ethereum (ERC20)**<br>`0x355d429f97511897ccb4e271ec888205f9ab6629` | <img src="../../coin/4.jpg" width="200" alt="Aptos"><br>**Aptos**<br>`0x836e3780edfc3f7b2372b39e2a1a3a5d7adfaccd96c726f21cfde1b50dd68030` |
| <img src="../../coin/5.jpg" width="200" alt="Plasma"><br>**Plasma**<br>`0x355d429f97511897ccb4e271ec888205f9ab6629` | <img src="../../coin/6.jpg" width="200" alt="Polygon POS"><br>**Polygon POS**<br>`0x355d429f97511897ccb4e271ec888205f9ab6629` |
| <img src="../../coin/7.jpg" width="200" alt="Solana"><br>**Solana**<br>`2hfhboHdmdrYsY25XfQSsEWxq5ip4EQsR7f4AzSRMUyr` | <img src="../../coin/8.jpg" width="200" alt="The Open Network (TON)"><br>**The Open Network (TON)**<br>`UQB9kFQohzmXUir9QSSZq01iwl9aQZIDdBpNmDklljRtCoGK` |
| <img src="../../coin/9.jpg" width="200" alt="Arbitrum One"><br>**Arbitrum One**<br>`0x355d429f97511897ccb4e271ec888205f9ab6629` | <img src="../../coin/10.jpg" width="200" alt="AVAX C-Chain"><br>**AVAX C-Chain**<br>`0x355d429f97511897ccb4e271ec888205f9ab6629` |

---

## License

MIT

Copyright (c) 2026 erik <erik@erik.xyz> — https://erik.xyz
