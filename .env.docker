# ============================================================
# Docker 环境变量
# Copyright (c) 2026 erik <erik@erik.xyz> — https://erik.xyz
#
# 使用方法: cp .env.docker .env && docker-compose up -d
# 注意: 所有 CHANGE_ME_ 开头的占位值（密钥/口令）部署前必须替换为强随机值，
#       应用启动时会强校验并拒绝占位值启动（fail-fast），切勿直接用于生产。
# ============================================================

# ── 应用配置 ──
APP_NAME=开放管理后台
# 运行环境: production=生产（DB 等口令强制非空校验） development=开发
APP_ENV=production
APP_DEBUG=false
APP_URL=http://localhost

# ── JWT 认证（生产密钥务必修改） ──
# 签名密钥，部署前必须替换为 64 位以上随机字符串（openssl rand -hex 32）
JWT_SECRET_KEY=CHANGE_ME_8a8d67aa4ade770cdb58439bb0976b0e7e0ad09f3ac5617f7d4234a79318cb0a
JWT_ALGORITHM=HS256
JWT_ISSUER=open-admin
JWT_AUDIENCE=open-admin
JWT_DEFAULT_EXPIRE=7200
JWT_REFRESH_EXPIRE=1209600

# ── Hashids ID 加解密（部署前必须替换为随机盐值） ──
HASHIDS_SALT=CHANGE_ME_73e3b545433e5dff4aa25c18cf8edfa1
HASHIDS_ALT_SALT=CHANGE_ME_c283acbf059217536161911a81a91359

# ── Snowflake ID 生成 ──
SNOWFLAKE_DATACENTER_ID=1
SNOWFLAKE_WORKER_ID=1
SNOWFLAKE_START_TIMESTAMP=1700000000000

# ── API 传输层加密（部署前必须替换为 32 字节随机密钥） ──
ENCRYPTION_KEY=CHANGE_ME_73073bc6f3b393c6146d129a808d1b23
ENCRYPTION_CIPHER=AES-256-CBC
ENCRYPTION_IV=

# ── 数据库存储层加密（部署前必须替换为 32 字节随机密钥） ──
ENCRYPTABLE_KEY=CHANGE_ME_e54e154b3752d727e7d81352f59f39a0
ENCRYPTABLE_CIPHER=AES-256-CBC
ENCRYPTION_PREVIOUS_KEYS=

# ── Elasticsearch（erikwang2013/webman-scout） ──
SCOUT_DRIVER=elasticsearch
SCOUT_HOSTS=http://elasticsearch:9200
SCOUT_PREFIX=erik_
SCOUT_SHARDS=1
SCOUT_REPLICAS=0
SCOUT_CHUNK_SIZE=500
SCOUT_SOFT_DELETE=true
# ES 安全认证，部署前必须替换为强随机口令（需与 docker-compose 中 ES 容器口令一致）
ES_USERNAME=elastic
ES_PASSWORD=CHANGE_ME_a07e62773b8da1c54c130b6bdf648be9

# ── 点击验证码 ──
POSTER_IMAGE_DRIVER=auto
POSTER_IMAGE_QUALITY=90
POSTER_CAPTCHA_STORAGE=redis
POSTER_CAPTCHA_TTL=300
POSTER_CAPTCHA_MAX_ATTEMPTS=3
POSTER_CAPTCHA_DIFFICULTY=medium

# ── 数据库连接（部署前必须替换 DB_PASSWORD / MYSQL_ROOT_PASSWORD 为强随机口令） ──
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=open_admin
DB_USERNAME=root
# 应用连接数据库的口令；生产环境（APP_ENV=production）禁止为空
DB_PASSWORD=CHANGE_ME_c0010a10b6f53a40022950dbdd7f374f
# MySQL 容器 root 口令，需与 DB_PASSWORD 保持一致
MYSQL_ROOT_PASSWORD=CHANGE_ME_ba57cc2d3466228cc37a542bbb5fcd59

# ── Redis 连接 ──
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DATABASE=0

# ── 队列连接 ──
# 队列驱动: redis / rabbitmq（docker-compose 未内置 rabbitmq 服务，使用 redis 即可）
QUEUE_DRIVER=redis
RABBITMQ_HOST=127.0.0.1
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
# RabbitMQ 口令，部署前必须替换为强随机口令（使用 rabbitmq 驱动时强制非空）
RABBITMQ_PASSWORD=CHANGE_ME_992d930ee690a60bcf9b29dc46a40bb3
RABBITMQ_VHOST=/

# ── Session 安全 ──
SESSION_TYPE=redis
SESSION_SECURE=true
SESSION_SAME_SITE=Lax

# ── CORS 跨域白名单 ──
CORS_ALLOWED_ORIGIN=http://localhost
